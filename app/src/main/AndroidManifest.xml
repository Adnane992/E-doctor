<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_doctor">
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Health_Care_Activity" android:launchMode="singleTop" />
        <activity android:name=".MainActivity2" android:launchMode="singleTop" />
        <activity android:name=".NotificationPane" android:launchMode="singleTop" />
        <activity android:name=".Medicament_Activity" android:launchMode="singleTop" />
        <activity android:name=".About_Activity" android:launchMode="singleTop" />
        <activity android:name=".Register_Activity" android:launchMode="singleTop" />
        <activity android:name=".Login_Activity" android:launchMode="singleTop" />
        <activity android:name=".MainActivity" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <receiver android:name=".ReminderBroadcast"/>
    </application>

</manifest>